{
  "name": "error-handler-lib-bcp",
  "module": "index.ts",
  "type": "module",
  "types": "./build/index.d.ts",
  "author": "Bruno Costa Prado",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BrunoCostaPrado/error-handler-lib.git"
  },
  "version": "0.0.6",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "bun run index.ts",
    "build": "tsup index.ts --format esm --out-dir build --clean && tsc",
    "lint": "biome check --fix .",
    "postinstall": "postinstall",
    "prepare": "postinstall"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.11",
    "@types/node": "^22.10.5",
    "del": "^8.0.0",
    "postinstall": "^0.7.5",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  },
  "optionalDependencies": {
    "@biomejs/biome": "^1.9.4"
  },
  "trustedDependencies": [
    "postinstall"
  ]
}
